[build]
builder = "NIXPACKS"

[build.nixpacks]
nixPkgs = ["libxml2", "libxslt"]

[deploy]
startCommand = "uvicorn app_v5.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
