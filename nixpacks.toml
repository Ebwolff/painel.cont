[phases.setup]
nixPkgs = ["libxml2", "libxslt"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd backend && uvicorn app_v5.main:app --host 0.0.0.0 --port ${PORT:-8000}"
